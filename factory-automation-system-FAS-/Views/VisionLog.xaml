<Window x:Class="factory_automation_system_FAS_.Views.VisionLog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="VisionLog" Height="900" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F6F7FB">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 상단 타이틀(유지) -->
        <Border Grid.Row="0" CornerRadius="12" Background="White" Padding="14"
                BorderBrush="#E5E7EB" BorderThickness="1">
            <DockPanel>
                <TextBlock Text="VISION LOG" FontSize="20" FontWeight="Bold" Foreground="#111827"/>
            </DockPanel>
        </Border>

        <!-- 단일 로그 리스트 -->
        <Border Grid.Row="2" CornerRadius="12" Background="White"
                BorderBrush="#E5E7EB" BorderThickness="1" Padding="12">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 간단 상태줄(원하시면 빼도 됨) -->
                <DockPanel Grid.Row="0">
                    <TextBlock Text="실시간 로그 리스트" FontSize="16" FontWeight="Bold" Foreground="#111827"/>
                </DockPanel>

                <DataGrid x:Name="dgVisionLog"
                          Grid.Row="2"
                          ItemsSource="{Binding Logs}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          RowHeight="74"
                          ColumnHeaderHeight="38"
                          Background="Transparent"
                          BorderThickness="0"
                          RowBackground="White"
                          AlternatingRowBackground="#FAFAFB"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto">

                    <DataGrid.Columns>
                        <!-- 타입 -->
                        <DataGridTextColumn Header="TYPE" Binding="{Binding Type}" Width="110"/>

                        <!-- 이미지 -->
                        <DataGridTemplateColumn Header="이미지" Width="170">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="10" Margin="6" Padding="2">
                                        <Image Source="{Binding ImagePath}" Stretch="UniformToFill"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- 데이터 -->
                        <DataGridTextColumn Header="데이터(내용)" Binding="{Binding DataText}" Width="*"/>

                        <!-- 시간 -->
                        <DataGridTextColumn Header="시간" Binding="{Binding Time}" Width="180"/>

                        <!-- 추가 필드(예: 방향/포지션 등) -->
                        <DataGridTextColumn Header="추가정보" Binding="{Binding Extra}" Width="220"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Window>
