<Window x:Class="factory_automation_system_FAS_.Views.VisionDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="제품 상세 검사 이력" Height="500" Width="900" 
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="상세 이력 (동일 바코드)" Grid.Column="0" Margin="5">
            <DataGrid x:Name="dgDetails" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True" 
                      SelectionChanged="dgDetails_SelectionChanged"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="검사시간" Binding="{Binding time_kst, StringFormat='yyyy-MM-dd HH:mm:ss.fff'}" Width="160"/>
                    <DataGridTextColumn Header="타입" Binding="{Binding type}" Width="60"/>
                    <DataGridTextColumn Header="측정치" Binding="{Binding ms}" Width="60"/>
                    <DataGridTextColumn Header="X" Binding="{Binding x}" Width="50"/>
                    <DataGridTextColumn Header="Y" Binding="{Binding y}" Width="50"/>
                    <DataGridTextColumn Header="판정" Binding="{Binding detected_class}" Width="*"/>
                    <DataGridTextColumn Header="컬러" Binding="{Binding color}" Width="60"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <GroupBox Header="이미지 미리보기" Grid.Column="1" Margin="5">
            <Border Background="Black" CornerRadius="3">
                <Image x:Name="imgDetailPreview" Stretch="Uniform" Margin="2"/>
            </Border>
        </GroupBox>
    </Grid>
</Window>