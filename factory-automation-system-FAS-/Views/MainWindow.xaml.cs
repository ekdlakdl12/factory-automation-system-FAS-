using System.Windows;

namespace factory_automation_system_FAS_.Views
{
    // 클래스 이름 앞에 partial이 반드시 있어야 합니다.
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            // 이제 빨간 줄이 사라질 것입니다.
            InitializeComponent();
        }
    }
}